<Window x:Class="FinalProject_Jenotan.UI.EditWindow.FormsWindow.FacultyForms.CRUDWindows.FacultyCheckOutsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalProject_Jenotan.UI.EditWindow.FormsWindow.FacultyForms.CRUDWindows"
        mc:Ignorable="d"
        Title="FacultyCheckOutsWindow" Height="450" Width="300" Background="#f9dcdc">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.15*"></ColumnDefinition>
            <ColumnDefinition Width="5*"></ColumnDefinition>
            <ColumnDefinition Width="0.15*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.15*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
            <RowDefinition Height="0.15*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Grid.Column="1" Background="#f9dcdc">
            <ListView ItemsSource="{Binding PendingCheckoutForms }"
   
                SelectedItem="{Binding SelectedPending, Mode=TwoWay}"
                 Background="#f5c3c3"
                 BorderThickness="0"
                 HorizontalContentAlignment="Stretch">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="#edb2b2"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Background="#edb2b2">
                            <StackPanel>

                                <TextBlock Foreground="Black"
                 FontSize="20" FontWeight="Bold"
                     Text="{Binding CheckoutId, StringFormat=Checkout Id: {0}}"  >
                                </TextBlock>
                                <TextBlock FontSize="15" FontWeight="SemiBold"
                    Text="{Binding RFormId, StringFormat=Reservation Id: {0}}" />
                                <TextBlock FontSize="15" FontWeight="SemiBold"
                    Text="{Binding EmployeeId, StringFormat=Employee Id: {0}}" />
                                
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </Grid>
        
    </Grid>
</Window>
