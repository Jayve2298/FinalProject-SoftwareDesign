<Page x:Class="FinalProject_Jenotan.UI.EditWindow.FormsWindow.FacultyForms.CompletionFormPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FinalProject_Jenotan.UI.EditWindow.FormsWindow.FacultyForms"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Background="#f9dcdc"
      Title="CompletionFormPage"  xmlns:cmd="clr-namespace:FinalProject_Jenotan.Commands"
         xmlns:converters="clr-namespace:FinalProject_Jenotan.Commands">

    <Page.Resources>
        <cmd:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <converters:BoolToStatusConverter x:Key="BoolToStatussColorConverter"/>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <converters:FacultyNameInitialConverter x:Key="FacultyNameInitialConverter"/>
        <converters:FacultyFullNameConverter x:Key="FacultyFullNameConverter"/>
    </Page.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.15*"></RowDefinition>
            <RowDefinition Height="9*"></RowDefinition>
            <RowDefinition Height="0.15*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            
            <TextBlock Text="Completed:" HorizontalAlignment="Left"
               VerticalAlignment="Bottom" FontSize="30"></TextBlock>

            <Grid Grid.Column="1">
                <ComboBox ItemsSource="{Binding SortOptions}"
                  SelectedItem="{Binding SelectedSort}"
                  Margin="180,30,0,0"/>
            </Grid>

            <Grid Grid.Column="3">
                
                <TextBox Margin="0,20,0,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Grid>
                                            <ScrollViewer x:Name="PART_ContentHost"/>

                                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                          Background="#f5c3c3"
                                          FontSize="18"
                                          HorizontalContentAlignment="Left"
                                          VerticalContentAlignment="Center"
                                          Padding="10,0,0,0"/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>
            
            
        </Grid>

        <Grid Grid.Row="2">
            <ListView ItemsSource="{Binding CompletionFormList }"
   
         SelectedItem="{Binding SelectedCompletion, Mode=TwoWay}"
          Background="#f5c3c3"
          BorderThickness="0"
          HorizontalContentAlignment="Stretch">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="#f5c3c3"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Background="#f5c3c3">
                            <StackPanel>

                                <TextBlock Foreground="Black"
                          FontSize="20" FontWeight="Bold"
                              Text="{Binding CFormId, StringFormat=Completion Id: {0}}"  >
                                </TextBlock>
                                <TextBlock FontSize="15" FontWeight="SemiBold"
                             Text="{Binding VehicleId, StringFormat=Vehicle Id: {0}}" />
                                <TextBlock FontSize="15" FontWeight="SemiBold"
                             Text="{Binding CheckoutId, StringFormat=Checkout Id: {0}}" />
                                <TextBlock FontSize="12"
                                Text="{Binding DateCompleted, StringFormat=Date Completed: {0}}"
                               />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </Grid>
    </Grid>
</Page>
